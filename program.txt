/* the program begins here. "main" starts by reading
a file name from the command line and reads it into an array*/
int main(int argc, char* argv[])
{
    //open the file
    FILE* inputfile;
    input_length=0;
    inputfile=fopen(arv[1],"r");
    // go through and count its length
    while(!feof(inputfile))
    {
        fgetc(inputfile);
        input_length++;
    }
    input_length--;	//undo eof
    //create a string to hold it
    input=(char*)malloc(input_length+1);
    //now go back through the file and read it again into the string
    rewind(inputfile);
    for(int i=0;i<input_length; i++)
        input[i]=fgetc(inputfile);
    input[input_length]=0;
    flocse(inputfile);
}